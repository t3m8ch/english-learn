---
entry: set out
type: phrasal-verb
---

# set out

## Значение 1: задаться целью и приступить

### Модель

`set out to + base verb`

### Когда используется

Выражение обозначает сознательное начало работы ради определённой цели: `set out to find` — «задаться целью найти».

### Контексты

- `After struggling with Paxos ourselves, we set out to find a new consensus algorithm that could provide a better foundation for system building and education. Our approach was unusual in that our primary goal was understandability: could we define a consensus algorithm for practical systems and describe it in a way that is significantly easier to learn than Paxos? Furthermore, we wanted the algorithm to facilitate the development of intuitions that are essential for system builders. It was important not just for the algorithm to work, but for it to be obvious why it works.`
  — После того как мы сами столкнулись с трудностями при работе с Paxos, мы решили найти новый алгоритм консенсуса, который мог бы стать лучшей основой для создания систем и обучения.
  — [Источник](../phrases/2026/09/2026-09-17-002.md)

### Типичная ошибка

`Set` — неправильный глагол с формами `set — set — set`, поэтому прошедшая форма `set out` внешне совпадает с начальной.
